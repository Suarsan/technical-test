import { FormInsertionDirective } from './form-insertion.directive';

describe('FormInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
